
pipeline {
      agent any
      stages {
            stage('checkout') {
                  steps {
                        // Clone the repository
                        git branch: 'main', url: 'https://github.com/akhila6522/MyWebApp.git'
                  }
            }
      }
}
